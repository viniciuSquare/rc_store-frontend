import { NgModule } from '@angular/core';
import { CommonModule } from '@angular/common';
import { Routes } from '@angular/router';

const routes: Routes = [
  {
    
  }
]

/***
 * ROUTES
 * ''                 -> stock relation
 * 'movements'        -> movements relations
 * 'movements/create' -> product new movement
 *
 * */

@NgModule({
  declarations: [],
  imports: [
    CommonModule
  ]
})
export class StockModule { }
